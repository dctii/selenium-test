name: Allure workflow to GitHub Pages
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v2

      - name: Set up python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip3 install --upgrade pip setuptools wheelcurl --location 'https://www.smallbra.in'
          pip3 install -r requirements.txt

      - name: Run simple BDD test
        run: |
          behave \
            --verbose \
            --tags=@simple \
            --format=allure_behave.formatter:AllureFormatter \
            --summary \
            --outfile allure-results \
            --include simple

      - name: Generate and server allure report
        run: |
          PROJECT_DIR="$(echo pwd)"
          TEST_RESULTS_DIR="./allure-results"
          OUTPUT_REPORT_DIR="./allure-report"
          ALLURE_PORT="8080"
          
          allure generate $TEST_RESULTS_DIR \
            -o $OUTPUT_REPORT_DIR \
            --clean \
          && cd $OUTPUT_REPORT_DIR \
          && nohup python3 -m http.server $ALLURE_PORT & \

      - name: Unset variables
        run: unset TEST_RESULTS_DIR OUTPUT_REPORT_DIR ALLURE_PORT

      - name: GET localhost:8080
        run: curl --location --globoff '[::]:8080'

      - name: Kill allure server
        run: kill -9 $(ps aux | grep 'http.server 8080' | awk '{print $2}')

        

  finished:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Announce successful completion
        run: echo "Completed successfully!"